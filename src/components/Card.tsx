import React from 'react'
/**
 * Card component 
 * Container for card data. On click swaps between question and answer.
 * Input based : (Insert answer) => Flip if right
 * Anki style : flip on click, buttons on bottom to 
 */
const Card = () => {
  return (
    <div>
      {/* Data from server */}

    </div>
  )
} 

export default Card; 